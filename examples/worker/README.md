# Nucleus Worker Template

Generated by:

```bash
nucleus init --template worker --name demo-worker --module example.com/demo-worker --dir ./demo-worker
```

The generated worker includes `nucleus.yaml`, minimal contracts, and a worker entrypoint.
