# Nucleus Library Template

Generated by:

```bash
nucleus init --template library --name demo-lib --module example.com/demo-lib --dir ./demo-lib
```

The generated library is intentionally runtime-free and can pass `go test ./...` without Nucleus runtime imports.
