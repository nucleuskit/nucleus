// Package capability implements CLI scaffolding for manifest-first capability
// declarations.
//
// The package is intentionally service-side orchestration code. It does not
// define public capability protocols, provider SDK APIs, or runtime defaults;
// those belong in cap, bridge, runtime, or generated service wiring.
package capability
